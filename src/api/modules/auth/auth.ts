import Http from '@/api/request'
import ApiUrls from '@/api/apiUrls'

/**
 * 登录接口
 * @returns 
 */
// export function getUserRoleInfo(postData: any){
//     return Http.get(ApiUrls.GetBattleInfoURL, postData)
// }